#include <stdio.h>
int conditional(int x, int y, int z) {
    // I don't know ☹️
}
int main(){
    int a,b,c;
    printf("Enter three numbers by spaces : ");
    scanf("%d %d %d",&a,&b,&c);
    printf("res = %d\n",(conditional(a,b,c)));
}